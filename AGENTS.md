# AGENTS.md

- Build via `devenv shell cargo ...` — no global `cargo` on PATH.
- **No code comments.** Zero `//`, `/* */`, `///`, `//!`, `TODO`, `FIXME` anywhere in source.
- **No commits unless explicitly asked.** Stage freely.
- **No fabricating plan items.** Only list what's actually in the plan.
